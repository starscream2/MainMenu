import streamlit as st

# --- PAGE CONFIG ---
st.set_page_config(page_title="T&T Real Estate Portal", page_icon="🇹🇹")

# --- CUSTOM CSS FOR BETTER BUTTONS ---
st.markdown("""
    <style>
    .stButton>button {
        width: 100%;
        height: 3em;
        font-size: 20px !important;
    }
    </style>
    """, unsafe_allow_html=True)

st.title("🇹🇹 T&T Real Estate Learning Portal")
st.write("Welcome! Please choose how you would like to study today:")

col1, col2 = st.columns(2)

with col1:
    if st.button("📝 Standard Practice Exam"):
        st.switch_page("pages/exam.py")

with col2:
    if st.button("💰 Real Estate Tycoon Game"):
        st.switch_page("pages/game.py")

st.divider()
st.info("Both options cover the Real Estate Agents Act 2020, RPO, and AML/CFT regulations.")
